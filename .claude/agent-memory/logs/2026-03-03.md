## Dave — 23:45
- **Issue**: #1 — Access to AL Compiler
- **Action**: Implemented `.claude/prerequisites.sh` to install the AL compiler (`microsoft.dynamics.businesscentral.development.tools.linux`) as a .NET global tool. The script is idempotent: installs .NET SDK if absent, installs the AL tool if not already present, and verifies the `AL` binary is accessible. The tool was already installed on this runner (`16.2.28.57946`), confirming the approach is correct.
- **Files changed**: `.claude/prerequisites.sh`
- **Status**: committed
